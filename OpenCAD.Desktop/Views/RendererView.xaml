<UserControl x:Class="OpenCAD.Desktop.Views.RendererView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:openGlRenderer="clr-namespace:OpenCAD.Kernel.Graphics.OpenGLRenderer;assembly=OpenCAD.Kernel.Graphics.OpenGLRenderer"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Image x:Name="image" />
    <!-- <Grid x:Name="LayoutRoot">

        <Grid x:Name="Overlay" Panel.ZIndex="1000" Visibility="Visible">
            <Grid.Background>
                <SolidColorBrush Color="Transparent" Opacity="1"/>
            </Grid.Background>
            <TextBlock x:Name="info"/>

        </Grid>

       
    </Grid>

   <openGlRenderer:OpenGLRenderer Model="{Binding Model}" />-->
</UserControl>
